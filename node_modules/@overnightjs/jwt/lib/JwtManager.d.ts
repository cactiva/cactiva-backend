/// <reference types="node" />
import { RequestHandler } from 'express-jwt';
import { Request } from 'express';
export interface ISecureRequest extends Request {
    payload: any;
}
export declare class JwtManager {
    private static readonly SECRET;
    private static readonly EXP;
    private readonly secret;
    private readonly expires;
    constructor(secret: string, expires: string | number);
    static jwt(dataToEcrypt: string | Buffer | object): string;
    jwt(dataToEcrypt: string | Buffer | object): string;
    private static setupJwt;
    static readonly middleware: RequestHandler;
    readonly middleware: RequestHandler;
    private static setupMiddleware;
}
